# 0001 — Hello, world!

The minimal Go program: a `main` package with a `main` function that prints
via the `fmt` package. The `go.mod` declares this directory a standalone
module so `go run .` works in isolation.

## Run

    go run .
