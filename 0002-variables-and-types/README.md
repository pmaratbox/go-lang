# 0002 — Variables & basic types

Declare bindings for Go's four basic types (`int`, `float64`, `string`,
`bool`) and print each via `fmt.Printf` with the appropriate verb. The
explicit `var name type = value` form is shown; the short form `n := 42`
would infer the same types.

## Run

    go run .
