# 0004 — Conditionals

Compare `n` against 10 and print whether it's less, equal, or greater. Go
forbids parentheses around the condition (gofmt convention) but requires
braces around the body, even for a single statement. Edit `n` to `10` or
`15` to exercise the other branches.

## Run

    go run .
