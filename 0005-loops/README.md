# 0005 — Loops

Print 1..5 with the canonical three-part `for` loop. Go has only `for` —
no `while`, no `do/while`. `for cond {}` acts as `while`, `for {}` is
infinite, and `for i, v := range slice {}` iterates collections.

## Run

    go run .
