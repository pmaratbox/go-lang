# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. Signature form is `func name(params) returnType`. When
consecutive parameters share a type, you can write it once: `a, b int`. Go
functions can return multiple values (e.g. `(int, error)`).

## Run

    go run .
