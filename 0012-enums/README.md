# 0012 — Enums

Define a `Color` enum (`Red`, `Green`, `Blue`) and print the integer value of
`Green` (1) and `Blue` (2). Go has no enum keyword — the idiom is a named
integer type plus a `const` block using `iota`, which auto-increments `0, 1, 2`
down the list.

## Run

    go run .
