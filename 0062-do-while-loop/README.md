# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. Go has no do-while; a bare `for { ... }` with a `break` at the bottom runs the body once before the condition is checked.

## Run

    go run .
