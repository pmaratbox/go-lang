# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. A two-pointer walk over `[]rune` compares the ends inward, stopping at the first mismatch — no reversed copy needed.

## Run

    go run .
