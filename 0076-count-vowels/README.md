# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `strings.ContainsRune` checks whether each rune is one of the vowels, incrementing the count on a match.

## Run

    go run .
