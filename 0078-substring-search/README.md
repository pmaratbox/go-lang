# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `strings.Index` returns the byte offset of the first match (`-1` if absent) — a byte index, which equals the character index for ASCII.

## Run

    go run .
