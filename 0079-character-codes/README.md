# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. A rune literal like `'A'` is its code point; `%d` prints the number and `%c` prints the character for a code.

## Run

    go run .
