# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. Reslicing `nums[i:i+size]` for each valid start gives the overlapping windows; the loop guard keeps each window full.

## Run

    go run .
