# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. `strings.Builder` is the efficient builder; it implements `io.Writer`, so `fmt.Fprintf` can format directly into it.

## Run

    go run .
