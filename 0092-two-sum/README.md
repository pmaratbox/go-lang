# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A `map[int]int` stores value to index; the comma-ok lookup of `target-n` finds the earlier complement in one pass.

## Run

    go run .
