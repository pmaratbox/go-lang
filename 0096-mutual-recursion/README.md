# 0096 — Mutual Recursion

Using two mutually recursive functions `isEven` and `isOdd` (each calling the other), report whether `4` and `3` are even, printing `even` and `odd`. Package-level functions see each other regardless of order, so the two recurse mutually toward the zero base case.

## Run

    go run .
