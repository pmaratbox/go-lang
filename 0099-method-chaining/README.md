# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Pointer-receiver methods return the same `*Calc`, so the calls chain and each mutates the shared struct.

## Run

    go run .
