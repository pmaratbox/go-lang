# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Early returns handle the special cases first; the final `return` is the default, keeping the function flat.

## Run

    go run .
