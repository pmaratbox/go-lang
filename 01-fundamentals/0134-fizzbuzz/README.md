# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A tagless `switch` reads cleanly for the cascading divisibility checks.

## Run

    go run .
