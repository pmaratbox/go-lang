# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. A package-level map caches results; the comma-ok lookup returns a cached value before recomputing.

## Run

    go run .
