# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. Go functions return multiple values natively, so a single pass yields both.

## Run

    go run .
