# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Appending the head after the reversed tail builds the result without any stdlib reverse.

## Run

    go run .
