# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. In Go, `type Meters int` defines a distinct named type carrying its own meaning.

## Run

    go run .
