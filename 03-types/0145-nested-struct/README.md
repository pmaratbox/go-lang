# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Go embeds one struct value inside another and reaches fields with chained dot access.

## Run

    go run .
