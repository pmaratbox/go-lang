# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. Go requires explicit conversions like `int(x)` and `float64(x)` between numeric types.

## Run

    go run .
