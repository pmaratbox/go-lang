# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. For comparable struct types Go's `==` compares every nested field structurally.

## Run

    go run .
