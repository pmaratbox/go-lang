# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. In Go, nil-pointer guards chained with `&&` provide the fallback.

## Run

    go run .
