# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. In Go, `iota` numbers the constants and a names slice maps ordinals back to names.

## Run

    go run .
