# 0346 — Newtype Wrapper

Wrap raw integers in distinct UserId and ProductId types so they cannot be confused, printing `user-1 prod-2`. In Go, defined types over int act as newtypes with their own String methods.

## Run

    go run .
