# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. Go has no classes or static methods; a package-level function is the equivalent of a class-level utility.

## Run

    go run .
