# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. Lowercase struct fields are unexported, so the balance stays private to the package.

## Run

    go run .
