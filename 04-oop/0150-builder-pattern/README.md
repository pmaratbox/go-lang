# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Pointer-receiver builder methods return `*PizzaBuilder` so the configuration calls chain fluently.

## Run

    go run .
