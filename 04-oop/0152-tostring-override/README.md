# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Implementing the `fmt.Stringer` interface with a `String()` method customizes how the value formats.

## Run

    go run .
