# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. `sort.Slice` takes a less function, so ordering by age stays a one-line comparator.

## Run

    go run .
