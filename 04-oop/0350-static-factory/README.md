# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. Go expresses a static factory as a package-level constructor function returning the value.

## Run

    go run .
