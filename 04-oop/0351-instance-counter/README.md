# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. Go uses a package-level variable incremented by a constructor function in place of a static field.

## Run

    go run .
