# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `sortedString` sorts a word's runes into a canonical string; two words match when their canonical forms are equal.

## Run

    go run .
