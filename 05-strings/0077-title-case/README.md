# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. `strings.Fields` splits on whitespace; each word's first byte is upper-cased and rejoined (`strings.Title` is deprecated).

## Run

    go run .
