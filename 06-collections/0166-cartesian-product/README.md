# 0166 — Cartesian Product

Print the cartesian product of [1,2] and ["a","b"] as `1a 1b 2a 2b`. In Go nested range loops with the first slice as the outer loop produce the pairs in order.

## Run

    go run .
