# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. In Go you tally counts in a map and use sort.SliceStable on first-seen order so ties keep their original ordering.

## Run

    go run .
