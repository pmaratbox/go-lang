# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. In Go a set built from a map[int]struct{} gives O(1) membership checks while preserving the first list's order.

## Run

    go run .
