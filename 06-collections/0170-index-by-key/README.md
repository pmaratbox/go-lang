# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. In Go a map[int]string built in one range loop gives instant lookups by id.

## Run

    go run .
