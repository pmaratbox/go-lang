# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Go uses pointer fields prev/next on a struct to link nodes both ways.

## Run

    go run .
