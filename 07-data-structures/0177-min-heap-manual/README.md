# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. Go backs the heap with a slice and hand-written sift-up/sift-down instead of container/heap.

## Run

    go run .
