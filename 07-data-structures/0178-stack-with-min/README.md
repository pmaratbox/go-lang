# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. Go keeps a parallel min-stack slice so getMin is a single index read.

## Run

    go run .
