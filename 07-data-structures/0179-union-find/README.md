# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. Go stores the disjoint-set parent array in a slice with path-halving in find.

## Run

    go run .
