# 0069 — Fibonacci (Iterative)

Print the first seven Fibonacci numbers, computed iteratively (each the sum of the previous two): `0 1 1 2 3 5 8`. `a, b = b, a+b` rolls the pair forward in one statement (the right side is evaluated first).

## Run

    go run .
