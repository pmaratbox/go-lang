# 0070 — Digit Sum

Sum the decimal digits of `1234` (repeatedly take the last digit with `% 10` and drop it with `/ 10`) and print the total: `10`. Integer `/` truncates, so `n /= 10` drops the last digit that `n % 10` just added to the total.

## Run

    go run .
