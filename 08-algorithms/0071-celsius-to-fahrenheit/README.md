# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. Integer `/` truncates, so `c*9/5 + 32` stays an `int`; the values divide evenly here.

## Run

    go run .
