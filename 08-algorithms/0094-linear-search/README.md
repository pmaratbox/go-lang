# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. A `for range` scans the slice until the target matches; the standard-library `slices.Index` does the same.

## Run

    go run .
