# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Idiomatic Go recurses over the remaining elements, keeping the input order for lexicographic output.

## Run

    go run .
