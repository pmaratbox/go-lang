# 0369 — Lower and Upper Bound

In the sorted array [1,3,5,5,7] find the lower bound (2) and upper bound (4) of 5, printing `2 4`. The stdlib `sort.Search` predicate handles both bounds idiomatically.

## Run

    go run .
