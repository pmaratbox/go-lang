# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. Go models enums with `iota` constants and a `String()` method for names.

## Run

    go run .
