# 0200 — Precondition Assert

Check a precondition arg>0: for 5 print `ok`, and for -1 report the failure `error: must be positive`, on two lines. Idiomatic Go returns an `error` value rather than panicking on a failed precondition.

## Run

    go run .
