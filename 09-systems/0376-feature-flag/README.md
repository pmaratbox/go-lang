# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A small helper keeps the same branch reusable for both flag values.

## Run

    go run .
