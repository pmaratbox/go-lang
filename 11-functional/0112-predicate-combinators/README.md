# 0112 — Predicate Combinators

Combine predicates with AND/OR/NOT: test `isEven AND isPositive` on 4 (yes) and -4 (no), printing `yes no`. Go closures over a `func(int) bool` type compose predicates cleanly.

## Run

    go run .
