# 0116 — Function Pipeline

Compose inc, double, and negate into a left-to-right pipeline and apply it to 3: ((3+1)*2) negated = `-8`. Go folds a variadic slice of functions left-to-right over the input.

## Run

    go run .
