# 0118 — Option Map Chaining

Map a function over a present optional (10 -> 12) and an absent one (-> fallback), printing `12 none`. A Go struct with a present flag models the Option, mapping only when present.

## Run

    go run .
