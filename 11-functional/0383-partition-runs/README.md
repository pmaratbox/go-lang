# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. In Go, iterate once and compare each element to its predecessor to start a new run.

## Run

    go run .
