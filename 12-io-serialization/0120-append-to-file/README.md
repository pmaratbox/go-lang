# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. The `os.O_APPEND` flag opens a file so writes go to the end without truncating.

## Run

    go run .
