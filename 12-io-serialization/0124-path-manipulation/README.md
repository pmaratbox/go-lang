# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. The `path` package always uses forward slashes, keeping output consistent across platforms.

## Run

    go run .
