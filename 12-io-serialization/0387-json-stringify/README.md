# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. Go's encoding/json marshals structs with field tags and emits compact output by default.

## Run

    go run .
