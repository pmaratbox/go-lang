# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. Go's encoding/json unmarshals into a map, and a fixed key slice keeps the output order deterministic.

## Run

    go run .
