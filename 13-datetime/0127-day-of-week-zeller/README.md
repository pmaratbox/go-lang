# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. A slice indexed by Zeller's `h` value maps the result to a full weekday name.

## Run

    go run .
