# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. In Go modular arithmetic over a name slice keeps the mapping clear.

## Run

    go run .
