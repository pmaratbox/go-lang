# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A `String()` method satisfies fmt.Stringer to yield the custom representation.

## Run

    go run .
