# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A closure captures the count variable, returning a pointer so the caller can read it.

## Run

    go run .
