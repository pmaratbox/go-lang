# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. The `%-*s` verb left-justifies the cell to a width computed at runtime.

## Run

    go run .
