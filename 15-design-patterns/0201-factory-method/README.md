# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. In Go a plain constructor function returning an interface is the idiomatic factory.

## Run

    go run .
