# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. An interface with several constructor methods is Go's natural abstract factory.

## Run

    go run .
