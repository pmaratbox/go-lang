# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. Copying a struct value in Go yields an independent deep copy for plain fields.

## Run

    go run .
