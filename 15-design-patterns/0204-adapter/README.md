# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter is a small struct that wraps the source and satisfies the target interface.

## Run

    go run .
