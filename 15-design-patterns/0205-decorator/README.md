# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each decorator embeds the wrapped component as an interface field and forwards to it.

## Run

    go run .
