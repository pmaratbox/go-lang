# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. Both leaf and composite satisfy one interface so callers treat them uniformly.

## Run

    go run .
