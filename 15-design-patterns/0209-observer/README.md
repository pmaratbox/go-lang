# 0209 — Observer

Notify two observers of a new value 5; each prints its id and the value on its own line. A subject holds a slice of observer interfaces and iterates to notify them.

## Run

    go run .
