# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. Go favors composition: the skeleton is a function taking an interface for the varying step.

## Run

    go run .
