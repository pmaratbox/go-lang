# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A small struct with hasNext/next methods carries the iteration cursor.

## Run

    go run .
