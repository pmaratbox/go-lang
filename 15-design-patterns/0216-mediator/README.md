# 0216 — Mediator

Have colleague A send "hi" through a mediator to colleague B, which prints `B got: hi`. Colleagues hold only a pointer to the mediator, which routes messages between them.

## Run

    go run .
