# 0217 — Memento

Save state 1, change it to 2, then restore from the memento, printing current then restored `2 1`. A memento value captures the state so restore can roll back later.

## Run

    go run .
