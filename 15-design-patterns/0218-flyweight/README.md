# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A map keyed by character caches and reuses shared glyph pointers.

## Run

    go run .
