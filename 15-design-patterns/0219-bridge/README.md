# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The shape holds a color interface, decoupling the abstraction from its implementation.

## Run

    go run .
