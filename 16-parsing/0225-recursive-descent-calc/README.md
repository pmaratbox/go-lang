# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. A small `parser` struct with pointer-receiver methods walks the grammar levels.

## Run

    go run .
