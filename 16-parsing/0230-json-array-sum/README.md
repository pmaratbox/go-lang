# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. `strings.Trim` peels the brackets before `strconv.Atoi` parses each element.

## Run

    go run .
