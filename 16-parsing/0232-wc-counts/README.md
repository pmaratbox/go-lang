# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. `strings.Fields` counts words and `len` over the string counts bytes.

## Run

    go run .
