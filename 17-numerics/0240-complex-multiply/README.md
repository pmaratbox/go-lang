# 0240 — Complex Multiplication

Multiply the complex numbers (1+2i) and (3+4i), printing the real and imaginary parts `-5 10`. Idiomatic Go uses the built-in `complex128` type with `real`/`imag`.

## Run

    go run .
