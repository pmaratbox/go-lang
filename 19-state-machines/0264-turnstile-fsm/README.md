# 0264 — Turnstile FSM

Drive a turnstile (locked/unlocked) with events coin, push, push and print the resulting states `unlocked locked locked`. A switch over state and event encodes the transitions idiomatically.

## Run

    go run .
