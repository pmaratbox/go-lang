# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. A small closure handles out-of-range cells as zero idiomatically.

## Run

    go run .
