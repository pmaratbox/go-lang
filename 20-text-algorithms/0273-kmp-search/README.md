# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Go builds the prefix-function over a byte slice and scans in a single pass.

## Run

    go run .
