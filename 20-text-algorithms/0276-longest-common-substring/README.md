# 0276 — Longest Common Substring

Find the longest common contiguous substring of "abcde" and "xbcdy", printing `bcd`. Go tracks the best run length in a DP slice and returns a substring slice of the source.

## Run

    go run .
