# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. Go uses a closure to expand around each center for both odd and even lengths.

## Run

    go run .
