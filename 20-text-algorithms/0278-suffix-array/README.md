# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Go sorts the index slice with `sort.Slice` comparing suffix slices directly.

## Run

    go run .
