# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Go keys a map by each word's sorted bytes and counts the distinct buckets.

## Run

    go run .
