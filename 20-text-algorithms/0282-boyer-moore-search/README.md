# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. Go stores the last-occurrence table in a byte-keyed map and shifts the pattern on mismatch.

## Run

    go run .
