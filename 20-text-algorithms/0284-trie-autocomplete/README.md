# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. Go models trie nodes with byte-keyed maps and a recursive DFS that visits children in sorted order.

## Run

    go run .
