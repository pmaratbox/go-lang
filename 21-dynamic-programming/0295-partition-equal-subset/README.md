# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. Go reduces to subset-sum on half the total with an early return for odd totals.

## Run

    go run .
