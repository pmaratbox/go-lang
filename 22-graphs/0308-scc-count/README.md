# 0308 — Strongly Connected Components

Count the strongly connected components of 0->1,1->2,2->0,2->3, printing `2`. Kosaraju's two DFS passes over the graph and its reverse count the SCCs idiomatically.

## Run

    go run .
