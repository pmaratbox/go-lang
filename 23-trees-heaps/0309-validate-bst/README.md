# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. A recursive helper carries `(low, high)` bounds idiomatically in Go.

## Run

    go run .
