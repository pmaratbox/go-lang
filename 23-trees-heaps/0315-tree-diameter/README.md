# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. A single DFS returns subtree height while updating a package-level best, an idiomatic Go pattern.

## Run

    go run .
