# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Go's standard `container/heap` with a `Less` that reverses ordering gives an idiomatic max-heap.

## Run

    go run .
