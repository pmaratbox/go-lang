# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. A bounded size-k min-heap from `container/heap` keeps only the k largest seen, idiomatic in Go.

## Run

    go run .
