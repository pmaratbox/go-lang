# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. A flat `2n` slice with iterative bottom-up query is the idiomatic, allocation-light Go form.

## Run

    go run .
