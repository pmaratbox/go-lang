# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. Higher-order functions returning closures let `compose` build the pipeline without naming its argument.

## Run

    go run .
