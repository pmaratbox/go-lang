# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. In Go the operator is a closure wrapping the source's Observer, calling next only when the predicate holds.

## Run

    go run .
