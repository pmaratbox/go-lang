# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. Per-source slices act as FIFO queues, draining a pair whenever both are non-empty.

## Run

    go run .
