# 0419 — Catch Error

Implement catchError that, on an error from the source, switches to a fallback stream. The error handler is a closure that resubscribes the same observer to the fallback Observable.

## Run

    go run .
