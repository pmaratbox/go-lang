# 0463 — Array of primitives

Serialize the integer slice `[]int{1, 2, 3}` to a compact JSON array using the standard library's `encoding/json` package via `json.Marshal`, which renders a Go slice as a JSON array with no extra whitespace.

## Run

    go run .
