# 0560 — SHA-512

Compute the SHA-512 hash of `hello` and print the lowercase hex digest. The standard library's `crypto/sha512` package implements the SHA-512 algorithm via `sha512.Sum512`, and `encoding/hex` formats the 64-byte digest as lowercase hexadecimal.

## Run

    go run .
