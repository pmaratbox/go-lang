# 0681 — Map

Uses the [samber/lo](https://github.com/samber/lo) functional library and its `lo.Map` transform to apply `x -> x*2` over `[1, 2, 3]`, then comma-joins the result.

## Run

    go run .
