# 0683 — Reduce

Uses the [samber/lo](https://github.com/samber/lo) functional library and its `lo.Reduce` transform to fold `[1, 2, 3, 4, 5]` with addition (init `0`), producing the single accumulated value.

## Run

    go run .
