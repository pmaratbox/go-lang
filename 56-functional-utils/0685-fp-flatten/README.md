# 0685 — Flatten

Uses the [samber/lo](https://github.com/samber/lo) functional library and its `lo.Flatten` transform to collapse `[[1,2],[3,4],[5,6]]` one level into a single slice, then comma-joins the result.

## Run

    go run .
