# 0687 — Zip

Uses the [samber/lo](https://github.com/samber/lo) functional library and its `lo.Zip2` transform to pair `[1, 2, 3]` with `["a", "b", "c"]`, formats each pair as `<n><s>`, then comma-joins the result.

## Run

    go run .
