# 0689 — Take

Uses the [samber/lo](https://github.com/samber/lo) functional library and its `lo.Subset` transform to take the first 3 elements of `[1..10]`, then comma-joins the result.

## Run

    go run .
